// TODO!
/*
import { addProviders, async, inject } from '@angular/core/testing';
import { BookListComponent } from './book-list.component';
import { BookStoreService } from '../shared/book-store.service';

xdescribe('Component: BookList', () => {

  // this fails because ngOnInit() is never executed
  it('should contain two example books', () => {
    let component = new BookListComponent(new BookStoreService());
    expect(component.books.length).toEqual(2);
  });
});
*/
