@Component({
  // ...
})
export class BookListComponent implements OnInit {
  books: Book[];
  @Output() <TEX>\bfseries showDetailsEvent</TEX> = new EventEmitter<Book>();

  <TEX>\bfseries ngOnInit()</TEX> {
    this.books = [
      // ...
    ]
  }

  // im HTML:
  // <a *ngFor="let b of books" [book]="b" (click)="showDetails(b)"></a>
  <TEX>\bfseries showDetails</TEX>(book: Book) {
    this.showDetailsEvent.emit(book);
  }
}
